# 增员（招募）平台开发方案 · 手把手实施手册

> 基于 CRM 现有底座（CloudBase PG 模式 `crm-d1gkae8ddc930d151`），融入友邦式增员画像与流程。
> 原则：**每阶段独立交付、可回退，绝不破坏现有可用状态**。所有改动先在本地完成并确认后再部署。

---

## 一、整体架构

```
┌─ 前端层：admin.html（上传为线上 index.html，只走 app.callFunction）
│   增员线索列表页 / 线索详情+时间线 / Super20 视图 / 招募仪表盘
├─ 服务层：云函数（源码 cloudfunctions/ → 部署产物 .deploy/<func>/）
│   recruits（线索CRUD）│ recruit_interviews（面谈）│ recruit_ai（AI评分）
│   ai_recommendations 扩展（增员建议）│ 招募统计接口
├─ 数据层：PG 数据库（云函数内 app.rdb() 免密）
│   新表：recruits / recruit_interviews / recruit_scores
│   复用：photos、ai_recommendations（加 type 字段区分客户新线索）
└─ AI 层：app.ai().createModel('cloudbase')
    hy3（文本：定级/十维评分/锦囊/话题）｜ hy3-preview（视觉：名片简历 OCR）
```

**复用策略（开发量小、风险低的关键）**：
- 新云函数直接套用 `customers` 的 CRUD + 排序分页模板、`photos` 的 base64 存储模式、`ai_recommendations` 的 listAll 架构
- 前端直接复用现有 `el()`、`makeSortHeader()`、`listState`、模态框、路由模式
- AI 直接复用 `ai_parse` 的模型调用与多模态消息格式（OpenAI 兼容协议）

**文件约定（动手前先确认）**：前端源文件统一改 `admin.html`，部署时上传为线上 `index.html`。⚠️ 注意：目前本地同时存在 `admin.html` 与 `index.html`（线上与本地 index.html 一致，与 admin.html 有极小格式差异）。**阶段1开工前需先确认以哪个为唯一源文件，建议把 index.html 内容同步回 admin.html 后只维护 admin.html。**

---

## 二、数据模型设计（阶段0 一次性建好）

### 1. `recruits` 增员线索表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | serial PK | 序号（可排序） |
| name / phone / gender / age | text/int | 基础信息 |
| occupation / education / prev_income | text | 职业层级、学历（本科/985211/海归）、既往年收入（友邦硬指标） |
| source_channel | text | 客户转介绍/缘故/陌生开拓/活动 |
| grade | text | **A（转型企业家）/ B（专业精英）/ C（高潜新人）** |
| funnel_stage | text | 破冰接触→事业说明→生涯规划面谈→决定性面谈→入职培训期 |
| stage_entered_at | timestamptz | 进入当前阶段时间（算滞留天数） |
| motivation | text | 转行动机摘录 |
| tags | text | 画像标签（逗号分隔） |
| ai_grade_reason | text | AI 定级依据 |
| is_super20 | boolean | 是否重点名单 |
| customer_id | int | 若从客户转化，关联客户表（互相跳转） |
| status | text | active / onboarded / dropped / parked |
| last_contact_date / next_contact_date | date | 轻接触节奏 |
| extra_info | text | 附加信息（AI 解析原文追加，同客户表惯例） |
| created_at / updated_at | timestamptz | |

### 2. `recruit_interviews` 面谈记录表

| 字段 | 说明 |
|---|---|
| id, recruit_id, interview_type | 破冰/事业说明/生涯规划/决定性 |
| interview_date, content | 面谈日期、记录原文（支持粘贴/OCR） |
| dimension_scores | **jsonb：十维胜任力评分**（企图心/爱心责任心/学习力/活动力/独立自主力/自我激励力/说服力/精神恢复力/柔韧性/感受力，各1-5） |
| risk_points | text | AI 标记的风险点（低分环节+原文佐证） |
| concerns | text | 对方异议（供锦囊生成） |
| conclusion | text | AI 综合结论（推进/暂缓/放弃建议） |

### 3. `recruit_scores` 画像评分表（AI 解析自动定级结果）

| 字段 | 说明 |
|---|---|
| id, recruit_id, scored_at | |
| five_factors | jsonb | 五要素：既往收入/职业层级/学历/年龄/转行动机 |
| total_score, grade | 评分与 A/B/C 结论 |
| source | text | manual（人工）/ ai_parse / interview |
| reason | text | 评分依据（原文摘录） |

### 4. 现有表改动（最小化）

- `ai_recommendations`：加 `recruit_id int` 列——建议类型自动区分客户建议 vs 增员建议，现有 AI 建议页零改动
- 客户详情页加"转为增员线索"按钮 → 调 `recruits.create` 带 `customer_id`，不动客户表结构

---

## 三、功能模块清单

| 模块 | 所属阶段 | 内容 |
|---|---|---|
| M1 线索池列表 | 阶段1 | A/B/C 分级徽章、五阶段标签、搜索/分页/排序（分级、阶段、最近接触、序号）、来源筛选、状态筛选 |
| M2 线索详情页 | 阶段2 | 基本信息编辑、五阶段进度条、时间线（接触/面谈/评分轨迹）、滞留天数提示 |
| M3 面谈记录 | 阶段2 | 按阶段的标准模板表单（事业说明关心点 / PASS 式评估提纲 / 异议记录） |
| M4 AI 画像定级 | 阶段3 | 复用 ai_parse：粘贴文本或名片/简历照片 → 结构化入库 + 五要素打分自动定 A/B/C |
| M5 AI 十维胜任力评分 | 阶段3 | 面谈记录 → 十维 1-5 分 + 原文佐证 + 风险点 + 结论；前端雷达图（Canvas 手绘，不引第三方库） |
| M6 AI 增员建议 | 阶段4 | 扩展 ai_recommendations：下一步动作建议、脱落预警（N 天未跟进置顶） |
| M7 异议锦囊 | 阶段4 | 输入异议 + 线索画像 → 生成针对此人的应答话术 |
| M8 Super20 视图 | 阶段4 | 列表筛选视图 + AI 定期生成轻接触话题建议 |
| M9 招募仪表盘 | 阶段5 | 漏斗各阶段人数、转化率、A/B/C 占比、画像分布（学历/年龄/收入区间） |
| M10 90天孵化档案 | 阶段6 | 新人周节点打卡（通关/首访/首单/首转介）、AI 成长计划、留存预警 |

---

## 四、手把手实施步骤（按阶段）

### 阶段0：数据地基（半天，零风险）

1. **建表**：写 `sql/create_recruit_tables.sql`（三张表的 CREATE TABLE + 索引：funnel_stage、grade、last_contact_date）。用 CloudBase 管理接口 `managePgDatabase` 执行。
2. **RLS 安全基线（必须）**：每张新表执行——
   ```sql
   CREATE POLICY recruits_fn_only ON recruits TO anon FOR ALL
     USING (current_setting('request.jwt.claims', true)::jsonb ->> 'sub' IS NULL);
   ```
   （与其他 6 张业务表同模式；**禁止** `USING(true)` 过宽策略）
3. **验证**：只查不写，确认现有 6 张表 policy 未被触碰、客户列表/详情/AI 功能全部正常。
4. ✅ 交付物：3 张空表 + 测试通过记录。此时系统与上线前完全等价。

### 阶段1：线索池（1-2 天）

1. **云函数 `recruits`**：从 `cloudfunctions/customers/index.js` 复制模板，改表名与字段：`create / get / update / remove / list`。list 支持 keyword、grade/funnel_stage/status 筛选、sortField/sortDir（null 排后逻辑照搬）、分页。
2. **部署**：`cp index.js db.js package.json → .deploy/recruits/` → `updateFunctionCode` → 管理接口直接 invoke 测试 create/get/list。
3. **前端**：admin.html 加主导航"增员"入口 → 新增 `showRecruits()` 列表页（复用 `makeSortHeader`、`listState` 模式，新建独立 `recruitState`）。列表列：姓名/分级/阶段/最近接触/下次接触/序号/操作。
4. **部署前端**：上传 admin.html → 线上 index.html → 强刷回归测试。
5. ✅ 验收：手工录入 3 条线索，搜索/排序/分页可用；客户列表与详情回归正常。

### 阶段2：流程漏斗与面谈（1-2 天）

1. 云函数 `recruit_interviews`（套 followups 模板）+ recruits 函数加 `advanceStage` action（只允许前进/回退一格，写 stage_entered_at）。
2. 前端线索详情页（复用客户详情页路由 `#/recruit/<id>`）：阶段进度条、时间线（合并 interviews + scores 按时间排序）、阶段标准模板表单（事业说明关心点 / 生涯规划 PASS 式评估提纲 / 决定性面谈异议记录）。
3. 部署 + 回归（重点：客户侧功能不回归测试不出新版本）。

### 阶段3：AI 画像定级 + 十维评分（2-3 天，核心亮点）

1. **扩展 `ai_parse`**：加 action `parseRecruit`——用现有多模态管线（文本或名片/简历照片），prompt 要求输出 JSON：结构化字段 + 五要素评分（0-10）+ 建议 grade 与依据。写 recruits + recruit_scores。
2. **新云函数 `recruit_ai`**：
   - `scoreInterview`：面谈记录 → 十维评分 JSON（要求每个分数附原文摘录）+ 风险点 + 结论 → 写 recruit_interviews.dimension_scores
   - `objectionScript`：异议锦囊（输入：异议内容 + 线索画像 + 历次面谈要点）
   - `topicSuggestion`：Super20 轻接触话题
3. 前端：详情页"AI 解析"入口（复用现有 AI 解析弹窗放大版）、面谈记录旁"AI 评估"按钮、Canvas 雷达图渲染十维评分。
4. 实测：用一段真实感面谈文本 + 一张名片照验证定级与评分质量，人工校准 prompt。
5. ✅ 验收：一张名片照 → 自动建线索 + 定级 + 依据可查。

### 阶段4：AI 建议与 Super20（1-2 天）

1. `ai_recommendations` 加 `recruit_id` 列与建议生成逻辑（脱落预警：`last_contact_date` 超 N 天自动生成置顶建议）。
2. 前端：AI 建议页加"增员"类型筛选（现有排序架构直接复用）；线索列表加 Super20 筛选开关；详情页"生成异议锦囊"按钮。
3. 部署 + 回归。

### 阶段5：招募仪表盘（1 天）

1. `recruits` 函数加 `stats` action：一次查询聚合出漏斗人数、阶段转化率、分级占比、画像分布（SQL 聚合，避免拉全量到前端）。
2. 前端工作台加"招募"页签：漏斗条形图（Canvas）、占比图、滞留线索列表（同阶段 >30 天）。

### 阶段6：90 天孵化（1-2 天，最后做）

1. `recruits` 表加 `onboarded_at`；节点打卡用 `recruit_interviews`（type=weekly_check）承载，不新建表。
2. AI 成长计划生成 + 连续无动作预警（进 ai_recommendations）。

---

## 五、每阶段通用操作手册（固定五步）

1. **改代码**：云函数改 `cloudfunctions/<func>/index.js`；前端改 `admin.html`
2. **同步产物**：`cp` 到 `.deploy/<func>/`
3. **部署**：云函数 `updateFunctionCode`；前端 Hosting 上传为 `index.html`
4. **验证新功能**：管理接口直接 invoke 云函数 → 前端强刷（Ctrl+Shift+R）实测
5. **回归老功能**：客户列表搜索/排序/分页、客户详情 7 个 Tab、AI 解析、照片——任一异常立即回退（本地保留每阶段改动前的 admin.html 备份，如 `admin_阶段0前.html`）

## 六、风险控制红线

- 不改 6 张现有业务表的 RLS policy；新表必须按基线建 policy
- 前端只走 `app.callFunction`；登录方式（crm_admin 密码登录）不动
- 云函数不动环境变量以外的配置；VISION_MODEL 默认值已在代码内固化（hy3-preview）
- 每阶段部署前备份 admin.html；部署后必须做客户功能回归
- AI 输出一律 JSON + try/catch 解析失败降级（不阻塞人工录入）
